Date created: September 2, 2026
Date last modified: September 2, 2026

# Register, Login, and Logout - Technical PRD

## Overview/Problem

QuizMaker is a greenfield application for teachers who need to collaborate on a shared bank of multiple-choice questions. Before any of that collaboration can happen, each teacher must be a distinct user in the system. Today the starter app has no user model, no database, and no way to register or sign in. This feature adds the minimum identity layer: a persisted user record, hashed passwords, and HTTP endpoints plus pages so a teacher can register, log in, log out, and land on a stub MCQ page that later sprints will build out.

---

## Hypothesis

We believe that a simple register / login / logout flow with hashed passwords and a user table will let multiple teachers become identifiable users of QuizMaker, which is the prerequisite for later collaborative test-bank work.

---

## Scope

### In Scope

- Cloudflare D1 database bound as `DB`, plus a `users` table created by a Wrangler migration
- User fields: primary key, first name, last name, username, email, hashed password, timestamps
- Username and email stored as separate columns; they may hold the same value for a given user
- Passwords hashed with SHA-256 (Web Crypto) on the client before they are sent in an HTTP POST; the database stores only the hash; login compares hashes, never plaintext
- A user service in `src/lib/services/` with create, find, update, and delete
- HTTP POST endpoints for register, login, and logout; register and login call the user service
- Register and login pages; a logout control; redirect to an MCQ stub page after successful register or login
- An MCQ stub page with no question-authoring UI
- Test-driven implementation with **Vitest**: each phase starts with failing tests, then production code until those tests pass

### Out of Scope

- Multiple-choice question create / edit / delete / list (next sprint)
- Social logins (Google, Microsoft, etc.)
- Tokens (JWT, API keys, refresh tokens)
- Session management, cookies, CSRF tokens, and any "remember me" behavior
- Protected-route middleware (the MCQ stub is reachable without a server-side session)
- Email verification, password reset, account lockout, rate limiting
- Roles, permissions, or teacher/admin distinctions
- HTTP endpoints for user update or delete (the service methods exist for later use; they are not exposed)

### Cut

- **bcrypt / Argon2** — extra dependency and native/WASM friction on Cloudflare Workers; SHA-256 via the Web Crypto API needs no new package and runs in both the browser and Workers
- **Server Actions for auth** — project convention prefers Server Actions for mutations, but this feature explicitly requires HTTP POST endpoints, so App Router route handlers are the chosen surface
- **Route protection on `/mcqs`** — cannot be done honestly without sessions or tokens, which are out of scope
- **`@cloudflare/vitest-pool-workers`** — would run tests on the real Workers runtime and change how the whole suite runs; unit tests mock D1 and `getCloudflareContext()` instead. Raise it only if a later sprint needs true Workers integration tests.

---

## Technical Requirements

### Database Schema

Cloudflare D1 (SQLite). Bind it as `DB` in `wrangler.jsonc`, regenerate types with `npm run cf-typegen`. Apply migrations with `--local` during development. Apply `--remote` only when the user explicitly wants the production database updated (required before register/login work on a newly created remote D1).

Worker name (`wrangler.jsonc` `name`): `quizmaker`. Public URL shape: `https://quizmaker.<account-subdomain>.workers.dev`. D1 database name: `quizmaker-2026`. Binding name must be `DB`. These two names are **not** the same string: the Worker is `quizmaker`; the database is `quizmaker-2026`.

```sql
CREATE TABLE users (
  id TEXT PRIMARY KEY DEFAULT (lower(hex(randomblob(16)))),
  first_name TEXT NOT NULL,
  last_name TEXT NOT NULL,
  username TEXT NOT NULL COLLATE NOCASE,
  email TEXT NOT NULL COLLATE NOCASE,
  password_hash TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX idx_users_username ON users (username);
CREATE UNIQUE INDEX idx_users_email ON users (email);
```

| Column | Type | Rules |
|---|---|---|
| `id` | TEXT PK | Random 16-byte hex UUID generated by SQLite |
| `first_name` | TEXT NOT NULL | Trimmed; 1–100 characters |
| `last_name` | TEXT NOT NULL | Trimmed; 1–100 characters |
| `username` | TEXT NOT NULL | Unique, case-insensitive; 3–254 characters (Zod). **May equal email** (e.g. `ada@school.edu`). Do not restrict to `[A-Za-z0-9_]+` — that would reject email-as-username |
| `email` | TEXT NOT NULL | Unique, case-insensitive; valid email format; may equal `username` |
| `password_hash` | TEXT NOT NULL | 64-character lowercase hex SHA-256 digest; never plaintext |
| `created_at` | DATETIME | Set on insert |
| `updated_at` | DATETIME | Set on insert; bumped on update |

Normalize `username` and `email` to lowercase before insert and lookup so uniqueness is predictable.

### API Endpoints

All auth routes are App Router route handlers under `src/app/api/`. They are server-only. They must not import client components. Validate every body with a Zod schema before calling the user service.

Request bodies use `passwordHash`, not `password`. The client hashes first; the server never receives the teacher's plaintext password.

#### POST /api/auth/register

**Request Body:**
```json
{
  "firstName": "Ada",
  "lastName": "Lovelace",
  "username": "alovelace",
  "email": "ada@school.edu",
  "passwordHash": "64-char-sha256-hex"
}
```

`username` and `email` may be identical (for example both `"ada@school.edu"`).

**Response:**
- Success (201): `{ "id": "...", "firstName": "...", "lastName": "...", "username": "...", "email": "..." }` — never include `passwordHash`
- Error (400): validation failure (missing fields, bad email, bad username, `passwordHash` not a 64-char hex string)
- Error (409): username or email already taken
- Error (500): unexpected server error

#### POST /api/auth/login

**Request Body:**
```json
{
  "username": "alovelace",
  "passwordHash": "64-char-sha256-hex"
}
```

The JSON field is `username`. Its value may be the teacher's **username or email**. Lookup uses `findByLoginIdentifier`: query `username = ?1` first, then `email = ?1` if that misses. Do not reuse one placeholder across `OR email = ?1` — D1 does not substitute the second occurrence, so email login fails while username login works.

A teacher with username `alovelace` and email `ada@school.edu` can sign in by typing either value in the Username field.

**Response:**
- Success (200): `{ "id": "...", "firstName": "...", "lastName": "...", "username": "...", "email": "..." }` — never include `passwordHash`
- Error (400): validation failure
- Error (401): unknown username/email or hash mismatch (same message for both: `"Invalid username or password"`)
- Error (500): unexpected server error

Compare hashes with a constant-time check so mismatch time does not leak which field failed.

#### POST /api/auth/logout

**Request Body:** none (empty JSON object is acceptable)

There is no session, cookie, or token to invalidate. This endpoint exists so the client has a single, explicit logout call. It always succeeds.

**Response:**
- Success (200): `{ "ok": true }`

After the response, the client navigates to `/login`.

### User Interface Requirements

Use the **shadcn/ui login and signup blocks** as the visual starting point (Card, Field, Input, Button, centered `min-h-svh` page shell). Styling is Tailwind via those components; do not add a separate CSS module or form library. Forms are client components because they must hash with Web Crypto before `fetch`. Do not add `react-hook-form`.

**Block files (paths match the shadcn examples):**
- `src/app/login/page.tsx` + `src/components/login-form.tsx`
- `src/app/register/page.tsx` + `src/components/signup-form.tsx`

**Adaptations from the stock blocks (required):** the blocks are email-centric and include Google and “forgot password”. Those conflict with this feature’s API and out-of-scope list. Keep the layout and component composition; change the fields and actions as below.

#### Home (/)

- Replace the starter landing content with a short QuizMaker intro and links to **Register** and **Log in**.

#### Register (/register)

- Page shell from the shadcn signup block (`flex min-h-svh w-full items-center justify-center p-6 md:p-10`, inner `max-w-sm`).
- `SignupForm` uses shadcn `Card` / `Field` / `Input` / `Button`.
- Fields (stock block had a single “Full Name” and no username; we split/add to match the user table):
  - First name, Last name, Username, Email, Password, Confirm password
- **Do not** include “Sign up with Google” (social login is out of scope).
- “Already have an account? Sign in” links to `/login`.
- Client validation before hashing:
  - All fields required
  - Password and confirm password match
  - Password at least 8 characters (checked on plaintext, then hashed)
  - Email looks like an email
  - Username at least 3 characters (may equal the email, e.g. `ada@school.edu`)
- On submit: hash the plaintext password with SHA-256, POST `/api/auth/register` with `passwordHash`, never send plaintext
- Success: navigate to `/mcqs`
- Failure: show the server or validation message on the form (`FieldError`); do not navigate

#### Login (/login)

- Page shell from the shadcn login block (same centered `min-h-svh` layout, inner `max-w-sm`).
- `LoginForm` uses shadcn `Card` / `Field` / `Input` / `Button`.
- Fields: **Username**, Password only. **Do not** add a separate Email field on login. The Username field accepts a username **or** an email (`type="text"` so a username without `@` is valid). Placeholder may hint both (e.g. `alovelace or ada@school.edu`). The POST body field is still `username`.
- **Do not** include “Forgot your password?” or “Login with Google”.
- “Don’t have an account? Sign up” links to `/register`.
- On submit: hash the password, POST `/api/auth/login` with `passwordHash` and the typed identifier in `username`
- Success: navigate to `/mcqs`
- Failure: show `"Invalid username or password"` (or the 400 validation message); do not navigate

#### MCQ stub (/mcqs)

- Placeholder page titled for the future question bank (for example "Question Bank")
- Short copy that this area will hold MCQ authoring in a later sprint
- A **Log out** control that POST `/api/auth/logout` then navigates to `/login`
- No question forms, lists, or database calls

#### Logout behavior

- No cookie clearing and no token revocation (neither exists)
- Logout is: call the endpoint, then send the teacher to `/login`

---

## Testing Approach (Vitest, TDD)

This feature is built **test-first**. Vitest is the unit-testing framework (see `.cursor/skills/testing/SKILL.md`). Do not write production code for a phase until that phase’s tests exist and have been observed failing.

### Red → green → next phase

For **every** phase below:

1. Write the tests listed in that phase. Tests must assert observable behavior (return values, status codes, DOM the user can see, SQL that was prepared). Do not write assertions that cannot fail.
2. Run `npm test`. The new tests must be **red** (fail because the module, route, or UI does not exist yet, or because behavior is wrong).
3. Implement the minimum production code for that phase.
4. Run `npm test` again. The phase’s tests must be **green**. Existing tests from earlier phases must stay green.
5. Only then mark the phase **COMPLETED**. Acceptance criteria for the phase are not met while its tests are red or skipped.

If a test is skipped (`it.skip` / `xit`) to make the suite green, the phase is not done.

### Harness (install once, at the start of Phase 1)

Vitest is not in the starter. The user has asked for it, so add these **dev** dependencies (no other test runner):

```bash
npm install -D vitest @vitejs/plugin-react@4 @testing-library/react @testing-library/user-event jsdom vite-tsconfig-paths
```

Pin **`@vitejs/plugin-react@4`**. Unpinned `plugin-react@6` fails `npm install` here (`ERESOLVE` against `@babel/core@7` from shadcn).

- `vitest.config.ts` at the repo root with `@vitejs/plugin-react`, `vite-tsconfig-paths` (required for the `@/` alias), `environment: "jsdom"`, and `globals: true`
- Scripts: `"test": "vitest run"` and `"test:watch": "vitest"`
- Colocate tests: `src/lib/password.ts` is tested by `src/lib/password.test.ts`
- `vi.clearAllMocks()` in `beforeEach` where mocks are used
- Mock `getCloudflareContext()` and D1; never hit a real database or network in unit tests
- Stub `server-only` with `vi.mock("server-only", () => ({}))` if a module imports it
- Query Testing Library by role and accessible name; use `userEvent` rather than `fireEvent`
- **`@testing-library/jest-dom` is not installed.** Do not use `toBeInTheDocument`, `toHaveTextContent`, or `toHaveAttribute`. Assert with `expect(el).toBeTruthy()`, `el.textContent`, and `el.getAttribute(...)`
- Auth route mocks must use `vi.hoisted(() => ({ ... }))`. A top-level `const createUser = vi.fn()` inside `vi.mock` throws `Cannot access ... before initialization`
- Form tests that hash then `fetch` must `await waitFor(...)` — SHA-256 is async
- Do not add `import "server-only"`; the package is not a direct dependency and Vitest cannot resolve it. Keep `src/lib/db.ts` and `src/lib/services/user.ts` off the client graph by never importing them from `'use client'` modules

### What we do not unit-test

- `npx wrangler d1 create` and applying migrations against a live local D1 file (manual / checklist in Phase 1)
- Server Components that only compose client children (test the client form/stub components)
- Visual layout and the end-to-end browser pass (still required in Phase 4–5 alongside green tests)

---

## Implementation Phases

### Phase 1: D1, users migration, and Vitest harness - COMPLETED

**Objective**: Persist users in a local D1 database, and have a working `npm test` so later phases can go red then green.

**Tests first (expect red)**:
1. Install the Vitest harness so `npm test` runs (this step is setup, not a behavior test).
2. Add `src/lib/users-table.contract.test.ts` that reads the SQL under `migrations/` and asserts:
   - a `CREATE TABLE` for `users`
   - columns `id`, `first_name`, `last_name`, `username`, `email`, `password_hash`, `created_at`, `updated_at`
   - unique indexes on `username` and `email`
3. Add a test that `wrangler.jsonc` contains a D1 binding named `DB` (string/file contract; do not call Wrangler from the test).
4. Run `npm test` and confirm these tests **fail** (no migration yet, or binding missing).

**Then implement until green**:
1. Create the D1 database with Wrangler (`npx wrangler d1 create quizmaker-2026`)
2. Add the `d1_databases` binding `DB` to `wrangler.jsonc`
3. Run `npm run cf-typegen` so `env.DB` is typed (do not hand-edit `cloudflare-env.d.ts`)
4. Create a migration for the `users` table and unique indexes
5. Apply the migration locally only: `npx wrangler d1 migrations apply quizmaker-2026 --local`
6. Re-run `npm test` until Phase 1 tests pass

**Phase gate**: `npm test` green for the contract tests. Local migration applied is a manual check (Wrangler), not a unit test.

**Deliverables**:
- Vitest config, `npm test` / `npm run test:watch`
- D1 binding in `wrangler.jsonc`
- Migration SQL under `migrations/`
- Typed `env.DB`
- Green `users-table.contract.test.ts`

### Phase 2: Password helper and user service - COMPLETED

**Objective**: Centralize hashing and all user table access.

**Tests first (expect red)**:

`src/lib/password.test.ts`:
- `hashPassword` returns a 64-character lowercase hex string
- the same plaintext always produces the same hash
- different plaintext produces a different hash
- the hash is not the plaintext
- `hashesMatch` is true for identical hashes and false for different ones

`src/lib/services/user.test.ts` (mock D1 / `getCloudflareContext()`; never a real database):
- `create` binds first name, last name, normalized lowercase username and email, and `passwordHash`
- `create` succeeds when username and email are the same value
- `findByUsername` returns the user without `passwordHash` / `password_hash`
- `findByUsername` returns `null` when no row exists
- login verification can obtain the stored hash (separate method) and does not leak it through `findByUsername`
- `findById`, `update`, and `delete` call the database as specified
- duplicate username or email is surfaced as a conflict the caller can map to 409 (not an untyped throw that becomes a 500 with no handling)
- SQL uses numbered placeholders (`?1`, `?2`), not string-concatenated user input

Run `npm test` and confirm these tests **fail**.

**Then implement until green**:
1. Add `src/lib/password.ts` — SHA-256 hex via `crypto.subtle.digest`, plus a constant-time compare helper; no `server-only` import so the browser can use it
2. Add `src/lib/db.ts` (or equivalent) that reads `env.DB` through `getCloudflareContext()` from `@opennextjs/cloudflare`
3. Add `src/lib/services/user.ts` with create, findByUsername, findById, update, and delete
4. Use prepared statements with numbered placeholders (`?1`, `?2`); never concatenate SQL
5. Never return `password_hash` from service methods used by HTTP responses; keep a separate lookup for login verification
6. Re-run `npm test` until Phase 1 and Phase 2 tests pass

**Phase gate**: password and user-service tests green. No HTTP routes yet.

**Deliverables**:
- Password helper usable on client and server
- User service as the only module that talks to `users`
- Green `password.test.ts` and `user.test.ts`

### Phase 3: Auth HTTP endpoints - COMPLETED

**Objective**: Register, login, and logout over HTTP POST.

**Tests first (expect red)**:

Call the exported `POST` handlers with `Request` objects. Mock the user service; do not open D1.

`src/app/api/auth/register/route.test.ts`:
- valid body → 201, JSON user fields, **no** `passwordHash` in the body
- `username` equal to `email` is accepted
- missing/invalid fields or `passwordHash` that is not 64-char hex → 400
- user-service conflict → 409
- unexpected throw → 500 without hash or stack in JSON

`src/app/api/auth/login/route.test.ts`:
- valid username + matching hash → 200, user fields, **no** hash in JSON
- unknown user or hash mismatch → 401 and `"Invalid username or password"` (same message for both)
- invalid body → 400

`src/app/api/auth/logout/route.test.ts`:
- POST → 200 and `{ "ok": true }`
- does not call the user service (nothing to invalidate)

Run `npm test` and confirm these tests **fail**.

**Then implement until green**:
1. Add **Zod** (not installed) because route-handler input must be schema-validated; do not add other production packages for this feature
2. Implement `POST /api/auth/register`, `POST /api/auth/login`, `POST /api/auth/logout`
3. Map D1 unique-constraint failures to 409
4. Ensure error JSON never includes hashes or stack traces
5. Re-run `npm test` until Phases 1–3 tests pass

**Phase gate**: all route-handler tests green.

**Deliverables**:
- Three route handlers
- Zod schemas for register and login bodies
- Green `route.test.ts` files for register, login, and logout

### Phase 4: Pages and MCQ stub - COMPLETED

**Objective**: Teachers can complete the flow in the browser.

Put interactive UI in client components under `src/components/` so Testing Library can render them. Pages in `src/app/` stay thin wrappers.

**Tests first (expect red)**:

Mock `fetch` and `next/navigation` (`useRouter`). Use `userEvent` and queries by role/label.

`src/components/signup-form.test.tsx`:
- renders first name, last name, username, email, password, confirm password
- client validation: empty fields, mismatched passwords, password shorter than 8 characters, bad email, short username — no `fetch`
- on valid submit, `fetch` is called with `POST /api/auth/register` and a JSON body that includes `passwordHash` (64-char hex) and **does not** include plaintext `password`
- 201 response → `router.push("/mcqs")`
- 409/400 → error text shown, no navigation
- no Google signup control

`src/components/login-form.test.tsx`:
- renders username and password; **no** separate email field
- valid submit POSTs `/api/auth/login` with `passwordHash`, not plaintext
- typing an email into Username still POSTs it as `username`
- 200 → `router.push("/mcqs")`
- 401 → `"Invalid username or password"`, no navigation
- exposes a link toward register
- no Google login or forgot-password control

`src/components/question-bank-stub.test.tsx`:
- shows question-bank stub copy, not MCQ authoring controls
- Log out POSTs `/api/auth/logout` then `router.push("/login")`

Run `npm test` and confirm these tests **fail**.

**Then implement until green**:
1. Build register and login from the shadcn signup/login blocks (adapted fields), plus `/register` and `/login` pages
2. Hash on submit, POST JSON, redirect to `/mcqs` on success
3. Build `/mcqs` stub with logout
4. Update `/` and root layout metadata (title/description) for QuizMaker
5. Re-run `npm test` until Phases 1–4 tests pass
6. Exercise the flow in the browser: register, login, logout, duplicate-user error, bad-password error (manual; complements tests)

**Phase gate**: component tests green **and** the browser pass above.

**Deliverables**:
- Working pages and client form/stub components
- Green form and stub tests
- Manual verification of happy path and error path

### Phase 5: Lint, full test suite, and build - COMPLETED

**Objective**: The feature is actually complete, not just visually present.

There are no new red tests in this phase. The “tests” here are the full existing suite plus lint/build gates.

**Tasks**:
1. Run `npm test` — **all** tests from Phases 1–4 must pass; zero skipped tests that were introduced to hide failures
2. Run `npm run lint` and fix issues
3. Run `npm run build` and report the actual result
4. Prefer `npm run preview` for D1/runtime checks (`npm run dev` will not catch Workers-only problems)
5. Re-run `npm test` after any lint/build fixes so the suite is still green

**Phase gate**: `npm test`, `npm run lint`, and `npm run build` all succeed.

**Deliverables**:
- Lint, test, and build results recorded in Current Status

**Phase 5 result (September 2, 2026)**:
- `npm test`: 9 files, 41 tests, all passing, 0 skipped
- `npm run lint`: exit 0 (eslint `.`, no findings)
- `npm run build`: exit 0, Next.js 16.2.12 Turbopack. Routes: `/`, `/login`, `/register`, `/mcqs` static; `/api/auth/register|login|logout` dynamic
- No lint/build fixes were required, so the suite was not re-run a second time
- User confirmed the flow works locally and on the deployed Cloudflare Worker

---

## Technical Implementation Details

### Key Files

- `wrangler.jsonc` — Worker `name` `quizmaker`; D1 binding `DB`, `database_name` `quizmaker-2026`, `database_id` `3ac801fa-2d58-4aa9-b504-ccd22af4745f`
- `migrations/0001_create_users.sql` — `users` table and unique indexes on username/email
- `src/lib/users-table.contract.test.ts` — migration / `DB` binding contract
- `vitest.config.ts` — Vitest + `@/` path resolution (`@vitejs/plugin-react`, `vite-tsconfig-paths`, `environment: "jsdom"`, `globals: true`)
- `src/lib/password.ts` — SHA-256 hash and constant-time compare (safe on client; no D1)
- `src/lib/db.ts` — `getDb()` via `getCloudflareContext({ async: true })`
- `src/lib/services/user.ts` — user CRUD; `UserConflictError` for unique violations; `findByLoginIdentifier` for username-or-email login
- `src/lib/auth/schemas.ts` — Zod register/login bodies (`passwordHash` 64-char hex)
- `src/lib/auth/http.ts` — `jsonError`, `readJsonBody`
- `src/app/api/auth/register/route.ts` — POST register
- `src/app/api/auth/login/route.ts` — POST login
- `src/app/api/auth/logout/route.ts` — POST logout (`{ ok: true }`, no user-service call)
- `src/lib/password.test.ts` — hash and compare
- `src/lib/services/user.test.ts` — user service against mocked D1
- `src/app/api/auth/register/route.test.ts` — register POST (`vi.hoisted` mocks)
- `src/app/api/auth/login/route.test.ts` — login POST
- `src/app/api/auth/logout/route.test.ts` — logout POST
- `src/components/signup-form.tsx` — shadcn signup block adapted for QuizMaker (tested)
- `src/components/login-form.tsx` — shadcn login block: Username + Password only; Username accepts username or email (tested)
- `src/components/question-bank-stub.tsx` — MCQ stub with logout (tested)
- `src/app/register/page.tsx` — registration page wrapper
- `src/app/login/page.tsx` — login page wrapper
- `src/app/mcqs/page.tsx` — MCQ stub page
- `src/app/page.tsx` — home; Register / Log in via `buttonVariants` + `Link` (do not invent Button `render` props)

### Cloudflare Worker and D1 (production)

| Resource | Value |
|---|---|
| Worker name | `quizmaker` |
| D1 name | `quizmaker-2026` |
| D1 binding | `DB` |
| D1 id | `3ac801fa-2d58-4aa9-b504-ccd22af4745f` |
| workers.dev URL | `https://quizmaker.<account-subdomain>.workers.dev` |

Account-level `workers.dev` subdomain (the `<account-subdomain>` part) is **not** in `wrangler.jsonc`. Cloudflare asks for it on first deploy. Valid names match `^[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?$` — **lowercase only**, no underscores, no leading/trailing hyphen. `Gangolu-bhramsi` is rejected; `gangolu-bhramsi` is accepted.

```jsonc
// wrangler.jsonc (relevant fields)
{
  "name": "quizmaker",
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "quizmaker-2026",
      "database_id": "3ac801fa-2d58-4aa9-b504-ccd22af4745f",
      "migrations_dir": "migrations"
    }
  ]
}
```

**Deploy commands** (`package.json`):

| Script | What it does |
|---|---|
| `npm run deploy` | OpenNext build + `wrangler deploy` (uploads Worker **and** POSTs `/workers/scripts/quizmaker/subdomain` to enable workers.dev) |
| `npm run upload` | OpenNext build + `wrangler versions upload` (uploads a version; **does not** POST the script subdomain) |
| `npm run preview` | OpenNext build + local Workers runtime |
| `npm run cf-typegen` | Regenerates `cloudflare-env.d.ts` |

If `npm run deploy` fails after a successful upload with API **10034** on `POST .../scripts/quizmaker/subdomain` (“verify your email”) while the dashboard already shows Verified, that is a Cloudflare account API mismatch, not an app bug. Workaround that succeeded: `npm run upload`, then enable the workers.dev route in the dashboard. `npx wrangler logout` / `login` did **not** clear 10034. GET `.../workers/subdomain` and GET `.../scripts/quizmaker/subdomain` returned 200; only the POST returned 403/10034.

Wrangler in a non-interactive agent shell cannot register a workers.dev subdomain (it defaults the prompt to **no**). First-time subdomain registration must happen in the user’s terminal or the [Workers onboarding](https://dash.cloudflare.com) UI.

Do not run `npm run deploy` unless the user explicitly asks. Do not apply `--remote` migrations unless the user explicitly wants production D1 updated. This sprint did both because the user asked to go live.

Create a new remote D1 (only when missing):

```bash
npx wrangler d1 create quizmaker-2026
# paste the printed database_id into wrangler.jsonc; keep binding "DB"
npx wrangler d1 migrations apply quizmaker-2026 --remote   # production only, when asked
npx wrangler d1 migrations apply quizmaker-2026 --local    # development
```

Keep `binding` as `"DB"`. Wrangler’s create snippet may suggest `"quizmaker_2026"` — ignore that; `getDb()` reads `env.DB`.

### Implementation Patterns

Password hashing (client on submit; same helper is valid on the server if ever needed):

```typescript
export async function hashPassword(plaintext: string): Promise<string> {
  const bytes = new TextEncoder().encode(plaintext);
  const digest = await crypto.subtle.digest("SHA-256", bytes);
  return Array.from(new Uint8Array(digest))
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}
```

D1 access and queries stay in `src/lib/`, not in components or `'use client'` modules:

```typescript
// src/lib/db.ts
import { getCloudflareContext } from "@opennextjs/cloudflare";

export async function getDb(): Promise<D1Database> {
  const { env } = await getCloudflareContext({ async: true });
  return env.DB;
}
```

```typescript
const { results } = await db
  .prepare("SELECT id, first_name, last_name, username, email FROM users WHERE username = ?1")
  .bind(username)
  .all();
const user = results[0];
```

Duplicate username/email: catch D1 `UNIQUE constraint failed` and throw `UserConflictError`; the register route maps that to 409.

Register/login client submit:

```typescript
const passwordHash = await hashPassword(password);
const response = await fetch("/api/auth/register", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ firstName, lastName, username, email, passwordHash }),
});
```

shadcn page shells (login and register):

```tsx
<div className="flex min-h-svh w-full items-center justify-center p-6 md:p-10">
  <div className="w-full max-w-sm">
    <LoginForm />
  </div>
</div>
```

Home links (this shadcn Button has no `render` / `asChild` API):

```tsx
import Link from "next/link";
import { buttonVariants } from "@/components/ui/button";

<Link href="/register" className={buttonVariants()}>Register</Link>
<Link href="/login" className={buttonVariants({ variant: "outline" })}>Log in</Link>
```

Zod bodies (`src/lib/auth/schemas.ts`): `username` is `z.string().trim().min(3).max(254)` so it may equal `email`. `passwordHash` is `/^[0-9a-f]{64}$/`.

Route-handler mock pattern:

```typescript
const { createUser } = vi.hoisted(() => ({
  createUser: vi.fn(),
}));

vi.mock("@/lib/services/user", () => ({
  createUser,
  UserConflictError,
}));
```

### Important Notes

- **No plaintext in transit or at rest (for this design).** The POST body carries `passwordHash`. The `users.password_hash` column stores that same value. This is not a substitute for TLS and is not a production-grade password scheme (SHA-256 is fast and unsalted). It is the agreed baseline for this sprint.
- **Client hashing does not create a session.** After 201/200 the UI navigates to `/mcqs`. Refreshing `/mcqs` does not "stay logged in" on the server because there is no cookie or token.
- **D1 is server-only.** Never import `src/lib/db.ts` or the user service into a `'use client'` module. The password helper is the exception because it has no binding access.
- **TDD is mandatory for Phases 1–4.** Write the phase’s tests, observe red on `npm test`, then implement until green. Do not implement a phase’s production code first and “add tests later.”
- **Approved test dependencies:** Vitest and the packages listed in Testing Approach. **Approved production dependency:** Zod for route-handler validation. Hashing uses the platform Web Crypto API. Ask before adding anything else.
- **Mock Cloudflare in unit tests.** `getCloudflareContext()` does not work under jsdom; supply a fake `env.DB`. Keep D1 behind `src/lib/` so tests mock that boundary.
- **Migrations default to `--local`.** Use `--remote` only when the user explicitly wants production D1 updated (done once this sprint when going live).
- **Do not deploy unless the user explicitly asks.** Prefer `npm run upload` if `npm run deploy` fails on workers.dev subdomain enable (10034). See Cloudflare Worker and D1 above.
- Unique indexes will throw on duplicate username/email; catch and return 409 rather than 500.
- **OpenNext on Windows** prints a WSL warning. Builds still succeed on this machine; prefer WSL if runtime failures appear.
- **PowerShell:** `curl` is `Invoke-WebRequest`. Use `curl.exe` or `Invoke-WebRequest` for HTTP smoke checks. `&&` is not a statement separator in older PowerShell; use `;`.

---

## Acceptance Criteria

- [x] A local D1 database exists, is bound as `DB`, and the `users` migration has been applied locally
- [x] A remote D1 `quizmaker-2026` exists, is bound in `wrangler.jsonc`, and the users migration has been applied remotely (production deploy)
- [x] A teacher can register with first name, last name, username, email, and password
- [x] Username and email may be the same value and registration still succeeds
- [x] The password is hashed in the browser before the POST; the network payload contains `passwordHash`, not the plaintext password
- [x] `users.password_hash` stores a 64-character hex SHA-256 digest, never plaintext
- [x] Successful register returns 201 (without the hash) and the UI navigates to `/mcqs`
- [x] Successful login returns 200 (without the hash) and the UI navigates to `/mcqs`
- [x] Login accepts username **or** email in the Username field (POST body uses `username`)
- [x] Login with a wrong password or unknown username/email returns 401 with `"Invalid username or password"`
- [x] Registering a duplicate username or email returns 409
- [x] Invalid payloads return 400
- [x] Logout POST returns 200 and the UI navigates to `/login`
- [x] `/mcqs` is a stub only — no MCQ authoring
- [x] User service supports create, update, delete, and the lookups login/register need
- [x] No cookies, tokens, or session records are introduced
- [x] Each of Phases 1–4 was implemented test-first: tests were red, then turned green
- [x] `npm test` (Vitest) passes with no skipped tests used to hide failures (9 files, 45 tests)
- [x] `npm run lint` and `npm run build` succeed

---

## Success Metrics

| Metric | Target | How Measured |
|--------|--------|--------------|
| Registration completion | A new teacher can create an account and reach `/mcqs` without errors | Manual pass on register happy path |
| Duplicate identity blocked | Second register with same username or email is rejected | 409 from `POST /api/auth/register` |
| Password confidentiality (baseline) | No plaintext password in DB rows or JSON bodies | Inspect D1 row and request payload |
| Time to first signed-in view | Under 2 minutes for a first-time teacher | Time from `/register` load to `/mcqs` |
| Unit tests | `npm test` exits 0 | Vitest in CI/local |

---

## Dependencies

### External Dependencies

- Cloudflare D1 — user persistence
- Web Crypto (`crypto.subtle`) — SHA-256 in browser and Workers (no npm package)

### Internal Dependencies

- `@opennextjs/cloudflare` `getCloudflareContext()` — D1 binding access
- shadcn/ui `button`, `card`, `field`, `input`, `label` — already installed
- **Zod (added)** — validate register/login bodies
- **Vitest (added, dev)** — `vitest`, `@vitejs/plugin-react@4`, `@testing-library/react`, `@testing-library/user-event`, `jsdom`, `vite-tsconfig-paths`

### Environment / config

- `wrangler.jsonc` `d1_databases` binding `DB`
- No new secrets for this feature; hashing is unsalted SHA-256 and needs no pepper for this sprint

---

## Risks and Mitigation

### Technical Risks

- **Risk**: `npm run dev` (Node) does not behave like Workers; D1 via `getCloudflareContext()` may fail or look fine locally and break on Workers.
- **Mitigation**: Verify register/login against `npm run preview`, not only `npm run dev`.

- **Risk**: Duplicate username/email surfaces as a raw D1 error (500).
- **Mitigation**: Catch unique-constraint failures in the user service or route handler and return 409.

- **Risk**: Importing the user service into a client component bundles or breaks D1 access.
- **Mitigation**: Client pages only `fetch` HTTP routes and import `hashPassword`. Database code stays in `src/lib/` used by route handlers.

- **Risk**: SHA-256 without salt is brute-forceable if the database leaks.
- **Mitigation**: Accepted for this sprint. A later auth hardening sprint can add a slow hash and a server-side salt/pepper. Do not pretend this is production auth.

- **Risk**: Unit tests that call real D1 or Wrangler are slow, flaky, and fail in environments without Cloudflare credentials.
- **Mitigation**: Mock `getCloudflareContext()` and D1. Schema is asserted by reading migration SQL, not by applying it in Vitest.

- **Risk**: Implementing UI or routes first, then retrofitting tests, skips the red signal this PRD requires.
- **Mitigation**: Phase gate is “tests were observed failing, then passing.” Do not mark a phase complete if tests were written after the implementation.

### User Experience Risks

- **Risk**: Teachers expect to stay logged in after refresh; without sessions they will not.
- **Mitigation**: Copy on `/mcqs` can stay neutral; do not imply a durable session. Next sprint may add sessions if needed.

- **Risk**: Logout appears to "do nothing" because there is no session to destroy.
- **Mitigation**: Always redirect to `/login` after logout so the action has a visible result.

---

## Troubleshooting Guide

Populate this section when issues are found during implementation.

### D1 binding missing at runtime
**Problem**: `env.DB` is undefined or queries throw.
**Cause**: Binding not in `wrangler.jsonc`, types not regenerated, or running on Node without the Workers context.
**Solution**: Confirm the `d1_databases` block, run `npm run cf-typegen`, apply migrations with `--local`, retry under `npm run preview`.
**Code Reference**: `wrangler.jsonc`

### Login works with username but not email
**Problem**: Typing the username signs in; typing the same account's email returns `"Invalid username or password"`.
**Cause**: D1 does not substitute a reused numbered placeholder. `WHERE username = ?1 OR email = ?1` binds the username column only.
**Solution**: Look up `username = ?1`, then `email = ?1` in a second statement.
**Code Reference**: `src/lib/services/user.ts` (`findByLoginIdentifier`, `findPasswordHashByLoginIdentifier`)

### Unique constraint on register
**Problem**: Register returns 500 when username or email already exists.
**Cause**: D1 error not mapped to 409.
**Solution**: Detect the constraint failure in the service/route and return 409 with a clear message.
**Code Reference**: `src/lib/services/user.ts` (`UserConflictError`), `src/app/api/auth/register/route.ts`

### Password POST still shows plaintext
**Problem**: DevTools request payload includes the raw password.
**Cause**: Form submitted before `hashPassword`, or the field was named `password` in the JSON body.
**Solution**: Hash, then send `passwordHash` only; keep the `<input type="password">` value in React state, not in the request body.

### `@/` imports fail in Vitest
**Problem**: Tests cannot resolve `@/lib/...`.
**Cause**: `vite-tsconfig-paths` missing from `vitest.config.ts`.
**Solution**: Add `tsconfigPaths()` to the Vitest `plugins` array as in `.cursor/skills/testing/SKILL.md`.

### `@vitejs/plugin-react@6` fails npm install
**Problem**: `npm install -D @vitejs/plugin-react` exits `ERESOLVE` (Babel 8 peer vs shadcn's Babel 7).
**Cause**: Latest plugin-react wants `@babel/core@^8`.
**Solution**: Install `@vitejs/plugin-react@4`.
**Code Reference**: `package.json`

### Wrangler not authenticated
**Problem**: `npx wrangler d1 create` / `npm run deploy` cannot run; Wrangler asks for `CLOUDFLARE_API_TOKEN` in a non-interactive shell.
**Cause**: No login on this machine, or the agent shell is non-interactive.
**Solution**: Run `npx wrangler login` in the user's own terminal (browser OAuth). Agent shells cannot complete that prompt. After login, `npx wrangler whoami` should show the account.
**Code Reference**: `wrangler.jsonc`

### workers.dev subdomain name rejected (re-prompt loop)
**Problem**: Wrangler keeps asking “What would you like your workers.dev subdomain to be?”
**Cause**: Name failed `/^[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?$/`. Capitals (`Gangolu-bhramsi`) and underscores (`Gangolu__`) are invalid.
**Solution**: Enter a lowercase name such as `gangolu-bhramsi`. This is the **account** subdomain, not the Worker name (`quizmaker`).
**Code Reference**: Cloudflare account setting; Worker URL is `https://quizmaker.<subdomain>.workers.dev`

### Deploy 10034 on script subdomain
**Problem**: `npm run deploy` uploads the Worker, then fails: `POST /accounts/.../workers/scripts/quizmaker/subdomain` code 10034 (“verify your email”) even when the dashboard shows Verified. `wrangler logout` / `login` does not fix it.
**Cause**: Cloudflare forbids enabling the workers.dev **route** on this Worker via API. GET account/script subdomain returned 200; only the POST returned 403.
**Solution**: Use `npm run upload` (skips that POST). Enable the workers.dev route in the Cloudflare dashboard for Worker `quizmaker`. File a Cloudflare support ticket if the dashboard is also blocked. Do not treat this as an app or D1 bug.
**Code Reference**: `package.json` scripts `deploy` vs `upload`

### D1 placeholder id on deploy
**Problem**: Deploy fails with code 10181: binding `DB` references `00000000-0000-0000-0000-000000000001` which was not found.
**Cause**: `wrangler.jsonc` still had the local placeholder id.
**Solution**: `npx wrangler d1 create quizmaker-2026`, put the real `database_id` in `wrangler.jsonc`, keep `"binding": "DB"`, apply `npx wrangler d1 migrations apply quizmaker-2026 --remote` when going live.
**Code Reference**: `wrangler.jsonc`

### `server-only` is not installed
**Problem**: `import "server-only"` fails Vitest (`Failed to resolve import "server-only"`).
**Cause**: The package is not a direct dependency; it only exists under Next's compiled tree.
**Solution**: Do not import `server-only` in this sprint. Keep `src/lib/db.ts` and `src/lib/services/user.ts` off the client graph by never importing them from `'use client'` modules.
**Code Reference**: `src/lib/db.ts`

### `vi.mock` factory ReferenceError
**Problem**: Auth route tests fail with `Cannot access 'createUser' before initialization`.
**Cause**: `vi.mock` factories are hoisted above `const createUser = vi.fn()`.
**Solution**: Create the spies with `vi.hoisted(() => ({ createUser: vi.fn() }))` and use those in the mock factory.
**Code Reference**: `src/app/api/auth/register/route.test.ts`

### `getCloudflareContext` throws in tests
**Problem**: User-service tests fail before assertions run.
**Cause**: The real OpenNext helper ran under jsdom.
**Solution**: `vi.mock("@opennextjs/cloudflare", ...)` and pass a fake `env.DB`.

---

## Notes for AI Agents

When working with this PRD:

1. Start by reading the Problem and Hypothesis to understand intent
2. Use Scope (In/Out/Cut) to determine boundaries — do not build out-of-scope items
3. Update phase status markers as work progresses
4. Add implementation details under "Technical Implementation Details" as code is written
5. Mark acceptance criteria as complete when features work
6. Add troubleshooting entries when bugs are found and fixed
7. Keep all sections current — remove outdated information
8. Use code references format: `filepath:line-number` when citing code
9. Do not implement MCQ authoring, social login, tokens, cookies, or sessions
10. Follow TDD per phase: write tests, run `npm test` (red), implement, run `npm test` (green). Do not skip the red run.
11. Vitest and Zod are already justified by this PRD. Ask before adding any other dependency. Do not add `@cloudflare/vitest-pool-workers` unless a later sprint asks for it.
12. Do not run `npm run deploy` or `d1 migrations apply --remote` unless the user explicitly asks. Default remains local migrations. If deploy fails on 10034 after a successful upload, use `npm run upload` and enable workers.dev in the dashboard (see Troubleshooting).
13. Do not edit `cloudflare-env.d.ts` or `package-lock.json` by hand
14. Verify with `npm test`, `npm run lint`, and `npm run build` before calling the feature done. All three must succeed.
15. Login identifier may be username **or** email, typed in the same Username field. The request field remains `username`. Server lookup is username `?1` first, then email `?1` — never `OR email = ?1` on one statement.
16. Pin `@vitejs/plugin-react@4`. Do not add jest-dom. Use `vi.hoisted` for route mocks. Use `waitFor` after client hashing.
17. shadcn Button here has no `asChild` / `render`. Style links with `buttonVariants()`.
18. This sprint is **done**. Do not start MCQ authoring unless a new PRD says so.

---

## Current Status

**Last Updated**: September 3, 2026
**Current Phase**: Phase 5 - Lint, full test suite, and build
**Status**: COMPLETED
**Next Steps**: None for this PRD. Later sprints may add MCQ authoring, sessions, or a slower password hash.

**Follow-up (September 3, 2026) — login by username or email**:
- Login form has **Username** and **Password** only. Do **not** show a separate Email field on `/login`.
- The Username field (`type="text"`) accepts a username **or** an email; POST body still uses `username`
- `findByLoginIdentifier` / `findPasswordHashByLoginIdentifier` query username `?1`, then email `?1` if needed
- `npm test`: 9 files, 45 tests, all passing
- Live: `https://quizmaker.gangolu-bhramsi.workers.dev` (Worker version `1250c502-1bd0-4470-bef5-407f541f514c`)
- Product decision: a two-field login (Username **and** Email) was tried and reverted. Keep one identifier field.

**Phase 5 verification**:
- `npm test`: 9 files, 41 tests, all passing, 0 skipped
- `npm run lint`: exit 0
- `npm run build`: exit 0 (Next.js 16.2.12 Turbopack; `/`, `/login`, `/register`, `/mcqs` static; auth APIs dynamic)
- User confirmed register/login/logout locally and on the deployed Worker
- Feature branch: `feature/register-login-logout`

**Phase 4 verification**:
- TDD: signup/login/stub suites failed (missing components), then passed
- `npm test` after Phase 4: 9 files, 41 tests, all passing
- shadcn login/signup block layout on `/login` and `/register`; Google and forgot-password removed
- Signup fields: first name, last name, username, email, password, confirm password
- Login field is **Username** (API contract `username`); the same field accepts email. Do not add a second identifier input.
- `/mcqs` stub with logout

**Phase 3 verification**:
- TDD: three route suites failed (missing `./route`), then passed after handlers were added
- `POST /api/auth/register` — 201 / 400 / 409 / 500 (no hash or stack in errors)
- `POST /api/auth/login` — 200 / 400 / 401 `"Invalid username or password"`
- `POST /api/auth/logout` — 200 `{ ok: true }`, no user-service calls
- Zod schemas in `src/lib/auth/schemas.ts`

**Phase 2 verification**:
- TDD: password and user-service suites failed (modules missing), then passed after implementation
- `hashPassword` / `hashesMatch` in `src/lib/password.ts` (no `server-only`; usable on client)
- D1 access via `getDb()` in `src/lib/db.ts`
- User service: `createUser`, `findByUsername`, `findPasswordHashByUsername`, `findById`, `updateUser`, `deleteUser`; duplicates throw `UserConflictError`

**Phase 1 verification**:
- TDD: 3 contract tests failed (no `migrations/`, no `DB` binding), then passed
- Local migration: `npx wrangler d1 migrations apply quizmaker-2026 --local` applied `0001_create_users.sql`
- `npm run cf-typegen`: `env.DB` is typed as `D1Database`
- Remote D1 was created later for production: `quizmaker-2026` / `3ac801fa-2d58-4aa9-b504-ccd22af4745f`; users migration applied `--remote`
